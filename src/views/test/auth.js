// import Cookie from "js-cookie";
// Cookies.set('foo', 'bar')\

//给浏览器设置cookie 获取它

// this.$cookie.set(name, value); //创建一个名为name 值为value的cookie
// this.$cookie.set(name, value, { expires: 7 }); //可以将cookie  保存七天
// this.$cookie.set(name, value, { path: "" }); // 创建一个带有路径的cookie

// this.$cookie.set(name, { year: 18 }); // 创建一个带有值为对象的cookie

// this.$cookie.get(name)
// //获取所有的cookie 
// this.$cookie.get();


// this.$cookie.remove(name) // 移出属性是name 对应的cookie 值

// this.$cookie.remove(); //移出所有的cookie